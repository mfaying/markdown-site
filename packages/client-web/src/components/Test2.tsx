import React from "react";
import { Input } from "zent";

const Test: React.FC = () => {
  const columns = [{
    title: "姓名",
    name: "name"
  }]

  const datasets = [{
    name: "xxx",
    id: 1
  }];


  return <Input />;
};

export default Test;