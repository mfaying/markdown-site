{
  "name": "webpack-doc",
  "private": true,
  "version": "1.0.0",
  "description": "webpack doc",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "xxx"
  },
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "cd packages/markdown-site-front && yarn build",
    "watch": "cd packages/markdown-site-front && yarn watch"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
